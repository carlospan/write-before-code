# 本项目 PRD

> **状态**：⬜ 骨架已就绪，**正文待填**。未填写的章节对 01 **无效**（不得把空标题当需求）。  
> **角色**：当前项目**唯一需求源**；绿场 01 的输入。  
> **怎么用**：按节填写；与本项目无关的节保留标题，正文写「本项目不涉及」+ 一句原因。  
> **编号**：功能用稳定编号 `F-1`、`F-2`…（01 的「覆盖 PRD 条目」会引用这些编号）。  
> **技术**：已定则写入；未定可写「由 01 推荐，经维护者确认」——不要留「待定」空话。  
> **棕地**：小修补走 `specs/`；只有产品范围变了才回写本文。

---

## 1. 定位与目标

### 1.1 一句话定位

（这个产品是什么、给谁用、解决什么问题。）

### 1.2 核心价值

- …

### 1.3 成功怎么算（可判定）

- [ ] …（尽量可观测；避免「体验好」「好用」）

### 1.4 明确不做（Out of scope）

- …
- …

---

## 2. 用户与场景

### 2.1 目标用户

| 角色 | 说明 | 主要目标 |
|------|------|----------|
| （例：终端用户） | | |
| （例：管理员） | | |

> 单用户 / 无账号体系：写清「本项目不涉及多用户」，并说明配置/数据归谁管。

### 2.2 关键使用场景

1. **场景 A**：…  
2. **场景 B**：…  

### 2.3 角色与权限（若有）

| 角色 | 可做什么 | 不可做什么 |
|------|----------|------------|
| | | |

> 无则写「本项目不涉及」。

---

## 3. 功能需求（按可交付模块组织）

> 每条功能一个编号。写**可观测行为**，不写实现细节（类名、表结构留给 01/02）。  
> 建议粒度：01 能直接拆成开发顺序里的模块；一条 `F-x` 不要跨太多无关能力。

### F-1 （模块/能力名）

- **做什么**：…  
- **谁用**：…  
- **主流程**：…  
- **边界 / 异常**：…  
- **验收意向**（可粗；细 WHEN/THEN 在 02 写死）：  
  - WHEN … THEN …  

### F-2 （模块/能力名）

- **做什么**：…  
- **谁用**：…  
- **主流程**：…  
- **边界 / 异常**：…  
- **验收意向**：  
  - WHEN … THEN …  

### F-3 …

（按需增删；保持编号稳定，不要中途随意改号。）

### 功能依赖与建议开发顺序（可选）

| 顺序 | 功能编号 | 依赖 | 备注 |
|------|----------|------|------|
| 1 | F-1 | — | |
| 2 | F-2 | F-1 | |

> 最终开发顺序以 01 的 `progress.md` 为准；此处仅表达产品侧依赖。

---

## 4. 数据与实体（概要）

> 只列**核心实体与关系**，不写完整字段（完整 Schema 在 02）。

| 实体 | 用途 | 主要归属功能 |
|------|------|--------------|
| | | F-x |

实体关系（一句话或 ASCII）：

```text
（例：User 1—N Order）
```

---

## 5. 接口与交互约束（若有）

> 已有全局约定则写死；没有则写「由 01 拟定草案，确认后进契约」。

- **客户端形态**：Web / 桌面 / CLI / 其他：…  
- **通信方式**：HTTP / RPC / IPC / 其他：…  
- **统一响应 / 错误表示**：… 或「01 拟定」  
- **鉴权**：无 / 有（方式与边界）：…  
- **实时 / 推送**（若有）：…  
- **外部服务**（若有）：用途、是否必须可切换、密钥不入仓：…  

---

## 6. 技术约束（若已定）

| 项 | 要求 | 备注 |
|----|------|------|
| 语言 / 运行时 | 已定 … / 由 01 推荐 | |
| 框架 | | |
| 数据存储 | | |
| 部署形态 | 单机 / 服务端 / 桌面内嵌 … | |
| 兼容性（浏览器/OS 等） | | |
| 禁止技术 / 禁止依赖 | | |

---

## 7. 非功能需求

> 能量化的尽量给数字；写进 PRD 后，01 会提炼进 `global-contract` 非功能基线。

| 类别 | 要求 | 量化（若有） |
|------|------|--------------|
| 性能 | | 如：P95 延迟 … |
| 可用性 / 稳定性 | | |
| 安全 | | |
| 隐私 / 合规 | | 无则「不涉及」 |
| 日志 / 可观测 | | |
| 国际化 | | 无则「不涉及」 |
| 其他 | | |

---

## 8. 环境、配置与交付（若有）

- **运行环境**：…  
- **配置 / 密钥**：如何提供；哪些严禁入仓：…  
- **构建 / 分发 / 更新**：… 或「不涉及」  
- **数据备份 / 迁移**：… 或「不涉及」  

---

## 9. 待确认与风险

> 歧义必须写在这里；不要让 Agent 静默假设。也可用：`[待确认：…]`。

| ID | 问题 | 影响 | 倾向（可选） | 状态 |
|----|------|------|--------------|------|
| Q-1 | | | | 未决 / 已定：… |

**已知风险**：

- …

---

## 10. 修订记录

| 日期 | 变更摘要 | 作者 |
|------|----------|------|
| YYYY-MM-DD | 建立骨架 | |

---

## 交给 01 之前的自检

- [ ] §1 定位与「不做」已写清  
- [ ] §3 至少有一条可交付的 `F-x`，编号稳定  
- [ ] 关键行为能用 WHEN/THEN 粗述（或已标待确认）  
- [ ] 技术未定项已标明「由 01 推荐」，无空洞「待定」  
- [ ] §9 未决问题已列出；无则写「无」  
- [ ] 已删除或改写所有仍是「示例/占位」的句子  

通过后：把本文交给绿场 **01**（见 `how-to/agent-prompts/01-overall-design.md`）。
